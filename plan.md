# MairiMichi 実施計画

- 最終更新: 2026-08-31
- 設計書: `docs/2026-08-31_MairiMichi_要件定義設計書_v1.md`
- 調査報告: `docs/2026-08-31_MairiMichi_外部データ源調査報告_v1.md`
- リポジトリ: https://github.com/zephel01/MairiMichi

---

## この文書の役割

**何を実装し、何を見送るか**を1枚で決める。設計書が「なぜそうするか」、これが「次に何をやるか」。

---

## 中核価値（1行）

> その願いに応える社寺を、なぜそう分類したかの根拠つきで示し、車がなくても行けるのかまで含めて判断できるようにする。

**空白**: 「御利益 × 地図 × アクセス難易度 × 御朱印」を全部やっているサービスは存在しない（調査済み・設計書 §14）。

---

## Phase 0 — 土台（現在地）

| # | タスク | 状態 |
|---|---|---|
| 0-1 | 設計書・調査報告のレビューと確定 | **レビュー待ち** |
| 0-2 | リポジトリ作成 | ✅ 完了 |
| 0-3 | Next.js 15 + Tailwind + Leaflet 雛形を HitoriYado から移植 | 未着手 |
| 0-4 | Cloudflare Workers (OpenNext) デプロイ設定 | 未着手 |
| 0-5 | Vitest / tsc --noEmit が緑 | 未着手 |
| 0-6 | D1 スキーマ定義（設計書 §11） | 未着手 |

**完了条件**: `npm test` と `npm run typecheck` が緑で、空の一覧ページが Workers 上で表示される。

---

## Phase 1 — よく参拝されている社寺 300〜500件を密に（MVP）

**選定基準は「大きい／有名」ではなく「よく行かれている」。** 規模は問わない。

**なぜ300〜500件か**: 自動導出で埋まらない3割（人物神18%・純ローカル神）と、原理的に自動化できない項目（参拝作法・御朱印の受付時間）を**手で埋め切れる件数の上限がその規模**だから。ここで作った手動データが Phase 2 以降の上書きテーブルになる。

### 1a'. 収録優先度の算出 ★先にやる（設計書 §4.5）

**「よく行かれている」を測る核は「正月比 ＝ 1月views ÷ 平常月views中央値」。** 実測がその妥当性を示している。

| 社寺 | 総views(8ヶ月) | 正月比 | 読み |
|---|---:|---:|---|
| 明治神宮 | 67,598 | **2.86** | 参拝地 |
| 浅草寺 | 61,858 | 2.03 | 参拝地 |
| **廣瀬大社** | **6,809** | **1.72** | **小規模だが伏見稲荷を上回る** |
| 伏見稲荷大社 | 66,015 | 1.59 | |
| **清水寺** | 90,709 | **1.17** | **純粋な観光地** |

**廣瀬大社（総views は伏見稲荷の1/10）が正月比で伏見稲荷を上回る。** これが「マイナーだが実際に参拝されている社」の検出器。

| # | タスク | 実装 |
|---|---|---|
| 1a'-1 | `etl/priority/p1_pageviews.ts` | **月次ダンプ**（`pageviews-YYYYMM-user.bz2`、約4.7GB）を落とす。per-article API はバッチ不可＋429頻発 |
| 1a'-2 | `etl/priority/p2_official_visitors.ts` | 47都道府県の観光入込客統計から地点別実数。**唯一の実測足数** |
| 1a'-3 | `etl/priority/p3_score.ts` | 加重和（views35% / **正月比30%** / 実測足数25% / sitelinks5% / 御朱印5%） |
| 1a'-4 | 記事なし社の別系統優先度 | 入込客統計掲載 / OSM `name` あり / 社格 / **近隣人口密度** |

**実装上の必須事項**
- 平常月は**平均でなく中央値**（ニュースのスパイクが混じる）
- **当月データは必ず除外**（2026-08 は全記事で異常値）
- 正月比は **3.0でクリップ**
- **`SitePriority` は D1 の公開テーブルにも API にも載せない。** 収録キューの並べ替えにのみ使う（設計書 §4.5.5）
- **収録の「可否」には使わない。** 可否は宗教法人リスト／OSM／Wikidata の和集合。分離しないと「Wikipedia に記事が無い＝存在しない」が構造化される（**神社の92.5%は記事が無い**）

### 1a. マスタ整備（人手）

| # | タスク | 成果物 |
|---|---|---|
| 1a-1 | 御利益タクソノミの確定（大分類10・細分類28） | `data/benefits.yaml` ✅ ドラフト済み |
| 1a-2 | 神格クラスタ 約40 の確定 | `data/deity_clusters.yaml` ✅ ドラフト済み |
| 1a-3 | クラスタ→御利益（primary/secondary）の確定 | `data/cluster_benefits.yaml` ✅ ドラフト済み |
| 1a-4 | 参拝作法の既定値＋例外の一次情報確認 | `data/etiquette_overrides.yaml` ✅ ドラフト済み（**全件 PENDING_VERIFICATION**） |
| 1a-5 | 巡礼グループの定義 | `data/pilgrimage_groups.yaml` ✅ ドラフト済み |
| 1a-6 | **対象300〜500件のリストアップ** | `data/phase1_sites.yaml` 未着手 |
| 1a-7 | 収録優先度ポリシーの確定 | `data/priority_policy.yaml` ✅ ドラフト済み |
| 1a-8 | 古典・史料の使い分けと権利整理 | `data/classical_sources.yaml` ✅ ドラフト済み |
| 1a-9 | **國學院デジタルミュージアムへ利用フォーム提出**（商用利用の意図を明示） | **未着手・クリティカルパス** |

**1a-4 は必ず公式サイトで裏取りしてから `source_type: official` に昇格させる。** 裏取りできなければ登録しない（既定値のみ表示）。

### 1b'. ETL: 典拠づけ（古典・史料）★差別化の核

**「祭神が◯◯だから御利益は△△」で止めない。「なぜその神がその御利益なのか」を一次史料までさかのぼって出典つきで書く。** 競合10サービスのどこもやっていない。しかも**古典の原文はパブリックドメイン**で、法的にもっとも安全な素材。

**★最大の盲点: `ja.wikisource` に古典の本文は無い。** 「古事記」「日本書紀」「延喜式」はいずれも版一覧のスタブで、`古事記 (原文)` `出雲国風土記` `日本三代実録` は404。**本文は全て `zh.wikisource`（中国語版）にある。**

| # | タスク | 取得元 | 権利 |
|---|---|---|---|
| 1b'-1 | `etl/classical/c1_wikisource.ts` | **zh.wikisource ダンプ**（`zhwikisource-latest-pages-articles.xml.bz2`）→ 延喜式全50巻／風土記5か国／六国史全巻／記紀／先代旧事本紀 | 原文PD |
| 1b'-2 | `etl/classical/c2_shikinai_parse.ts` | **神名帳（巻9・10）のパーサ**。式内社2,861社を 社格（名神大/大/小）・官幣国幣・月次新嘗相嘗 つきで構造化 | 原文PD |
| 1b'-3 | `etl/classical/c3_kokugakuin.ts` | 國學院 式内社DB（論社比定・座標）＋ 神社史料集成 古代（約450社の神階）／現代（約1,500社の近代社格） | 利用申請不要・商用可 |
| 1b'-4 | `etl/classical/c4_aozora.ts` | 青空文庫 武田祐吉『校註古事記』(card51731)『現代語譯古事記』(card51732) | **PD**（1958年没・2009年PD化・70年延長で復活せず） |
| 1b'-5 | `etl/classical/c5_ndl_fulltext.ts` | NDL次世代デジタルライブラリー API（保護期間満了の県神社誌・郡誌の全文検索） | 保護期間満了資料のみ |
| 1b'-6 | `etl/classical/c6_deity_index.ts` | 神名リストで全巻grep → 「この神がこの巻のこの条に出る」索引を自作（**既製の機械可読索引は存在しない**） | — |

**パーサの山場（1b'-2）**: 割注が `羽束師坐高御産日神社／大。月次新甞。∥` という独自記法。加えて **`〓`（欠字）と `■` が散在**し数値の一部が欠けている（冒頭の総数規定も「天神地祇惣三千一百〓二座」）。`貞コ`（おそらく「貞観」）のような文字コード変換の残骸もある。**比定社は含まれない**ので國學院の式内社DBで突合する。

**早めに動くこと**: 國學院デジタルミュージアムには **API も一括DL も無い**。商用サービスに組み込むなら**利用フォームで意図を明示して提出**する。ここが Phase 1 のクリティカルパスになりうる。

### 1b. ETL: 位置・祭神・御利益

| # | タスク | 外部依存 |
|---|---|---|
| 1b-1 | `etl/01_fetch_osm.ts` — Geofabrik PBF から社寺抽出 | japan-latest.osm.pbf (2.3GB) |
| 1b-2 | `etl/02_fetch_wikidata.ts` — SPARQL で P825/P625/P131/P571 | query.wikidata.org |
| 1b-3 | `etl/03_fetch_jawp.ts` — infobox 祭神/本尊/宗派/札所等 | jawiki ダンプ (4.71GB) |
| 1b-4 | `etl/04_normalize_deity.ts` — 神格クラスタへ正規化 | `data/deity_clusters.yaml` |
| 1b-5 | `etl/05_derive_benefit.ts` — 御利益導出（primary/secondary） | `data/cluster_benefits.yaml` |
| 1b-6 | `etl/06_apply_overrides.ts` — 手動データの上書き | `data/*.yaml` |

**完了条件**: 対象全件に御利益が付与され、**全件に導出根拠（どの祭神からか）が紐づいている**。未同定は `unknown` として件数が説明できる。

**実装上の落とし穴（設計書 §16・調査報告 §1 に詳細）**
- `name:ja` ではなく `name` を主キーにする（`name:ja` は充足率6%）
- `amenity=place_of_worship` で必ず絞る（`religion=shinto` だけだと鳥居12,000件が混入）
- パイプリンクは**リンク先記事名**を正規化キーに使う（表示名の揺れ41.2%を吸収）
- 座標の記法は `{{coord}}` / 度分秒個別 / `{{WikidataCoord}}` の3パターン全てに対応
- Wikidata の同一神格の別QID分裂（八幡⇔応神、稲荷⇔ウカノミタマ等）を必ずクラスタで吸収

### 1c. ETL: 交通・標高

| # | タスク | データ源 |
|---|---|---|
| 1c-1 | `etl/07_build_transit.ts` — 駅・バス停・GTFS便数 | N02-25 / P11-22 / P36-2023 / gtfs-data.jp / ODPT |
| 1c-2 | `etl/08_compute_access.ts` — accessMode / walkLoad / 累積標高 | Valhalla（Phase 1 は公開インスタンスを低速で）／地理院 DEM タイル |

**完了条件**: 全件に `accessMode` と `walkLoad` が付き、**`UNKNOWN` の件数とその理由（GTFS未整備の地域名）が説明できる**。

**必須の注意**
- **ODPT を必ず入れる。** 京都市営バスの GTFS は gtfs-data.jp に無く ODPT にしかない。入れないと京都の大半が誤判定になる
- N07 バスルートは使わない（2022年版で運行頻度が削除、2011年版は非商用）
- 標高は標高APIではなく **DEM タイル（PNG）** を使う。5A→5B→5C→10B のフォールバック実装
- Google/Mapbox の経路APIは**結果のDB保存が規約違反**なので使わない

### 1d. 手動キュレーション

| # | タスク | 件数 |
|---|---|---|
| 1d-1 | 参拝作法の一次情報確認 | 例外がある社のみ |
| 1d-2 | 御朱印の実務情報（有無・受付時間・昼休み・直書き/書き置き・初穂料・無人か） | 300〜500件 |
| 1d-3 | 人物神・純ローカル神の御利益を公式記載から個別登録 | 約3割 |
| 1d-4 | 巡礼グループの所属登録 | — |

**`verifiedAt` を必ず記録する。** UI で「◯ヶ月前の情報」と出し、公式サイトへのリンクを併記する。

### 1e. UI 実装

F-01〜F-13（設計書 §4.1）。優先順:

1. 御利益から探す＋**導出根拠の表示**（F-01/F-02/F-03）— これが中核
2. **アクセス難易度の表示と絞り込み**（F-05/F-07）— これが差別化
3. 地図（F-06）— HitoriYado の `HotelMap.tsx` を移植
4. 地域から探す（F-04）
5. 参拝作法（F-08）・御朱印（F-09）・巡礼グループ（F-10）
6. 経路リンク（F-11）・参拝記録（F-12・ローカル保存）・外部評判リンク（F-13）

### 1f. 実地検証 ★必須

**実際に数社を参拝し、アクセス判定と御朱印情報が現実と合うか確認する。**

HitoriYado が実データ検証で楽天APIの癖を7つ見つけたのと同じ構造。アクセス判定の閾値（徒歩上限・便数閾値・登り標高）は机上では決まらない。

- 検証対象: `TRAIN_ONLY` / `TRAIN_BUS` / `CAR_ONLY` / `UNKNOWN` を各1件以上
- 御朱印の受付時間が実際と合っているか
- 徒歩負荷 `HARD` の社が本当にきついか

---

## Phase 1.5 — 穴埋め（設計書 §4.6）

**穴は主観ではなく、クエリの結果で機械的に検出する。** `etl/10_detect_gaps.ts` を走らせ、`_OUTPUTS/gaps_YYYY-MM-DD.md` を出力する。**0件になる組み合わせの一覧が、そのまま次の収録キュー。**

| 穴の種類 | 検出クエリ | 例 |
|---|---|---|
| 地理の穴 | 都道府県 × 収録件数 が閾値未満 | 「島根県が3件しかない」 |
| 御利益の穴 | 地方 × 大分類 で0件 | 「四国に『安産・子授け』が0件」 |
| アクセスの穴 | 地方 × `accessMode ≠ CAR_ONLY` で0件 | 「この県に車なしで行ける社が0件」 |
| 御朱印の穴 | 地方 × `goshuin.available = yes` で0件 | 御朱印帳ユーザーの行き先が無い |
| 巡礼の穴 | グループの構成社が欠けている | 「四国八十八箇所が62件しか入っていない」 |
| 作法の穴 | PENDING_VERIFICATION が残っている | 一次情報の裏取り待ち |

**巡礼グループの穴は特に重要。構成社が欠けたグループは「めぐりコース」機能（Phase 2）が成立しない。** グループ単位で「N件中M件収録済み」を管理する。

---

## Phase 2 — 1地方へ拡張

| # | タスク |
|---|---|
| 2-1 | 対象を関東 or 近畿へ拡大（3,000〜5,000件） |
| 2-2 | 自前 Valhalla（Docker）を立て、徒歩距離を全件バッチ計算 |
| 2-3 | **めぐりコース**（グループ × アクセス判定 × 御朱印受付時間で「1日で回れるか」） |
| 2-4 | 地域分割UI・エリア定義の実用性検証 |

**めぐりコースが v2 の目玉。** 競合が誰もやっていない組み合わせ。

---

## Phase 3 — 全国

| # | タスク |
|---|---|
| 3-1 | 全国展開（30,000件〜）。自動導出中心に切り替え、「不明」を正直に出す |
| 3-2 | 参拝記録のアカウント同期 |
| 3-3 | ユーザー投稿による情報補完（受付時間の更新など） |
| 3-4 | リクスタの有償データ（15万件・祭神/宗派つき）の見積もり取得を検討 |

---

## やらないこと（明示的に見送る）

| 項目 | 理由 |
|---|---|
| **社寺のランキング・総合スコア（表示）** | 宗教施設に機械が優劣を付けるのは不適切。根拠も持てない。自己強化ループでロングテールが埋もれる（設計書 §9）。※**収録優先度は内部でのみ持つ。UIには出さない** |
| **自前の口コミ・評点** | 同上。外部（Google マップ、ホトカミ）へ送客する |
| **「効果がある」という主張・示唆** | **検証不可能**。「そう語られている」という事実を出典つきで引用するに留める（設計書 §9.1.5 の層A/B/C） |
| **体験談の自由記述（MVP）** | モデレーションコストと責任が重い。**MVPは「何を願ったか」のタグ集計まで**（＝効果ではなく意図の観測）。自由記述は v2 でガード付きで検討（設計書 §9.1.6） |
| **縁切りの手順・呪法の掲載** | 他者への加害に転びやすい。**縁切りカテゴリには公的な相談窓口への導線を常設する**（設計書 §9.1.6） |
| **文化遺産オンラインのテキスト取り込み** | All Rights Reserved で明示的に禁止。リンクのみ |
| **ADEAC の自動スクレイピング** | 本文がJS難読化されており、430KBのページからHTTP取得で取れるのは194文字。意図的に自動収集を防いでいると解釈すべき |
| **岩波・小学館・中公等の現代語訳の掲載** | 訳者に著作権。引用の範囲のみ。**武田祐吉版（PD）を使う** |
| **怪異・妖怪伝承DB の利用** | 利用条件未確認。運営自身が「現代の基準に照らせば不適切な表現を原資料のまま公開」と告知 |
| **ネットの初詣参拝者数ランキング** | **2009年警察庁データの孫引きと実証済み**（tabi-mag.jp の値と2009年データが順位構造・値ともほぼ一致）。しかも警察庁の数字自体が寺社の自己申告 |
| **Google Places の `rating` / `userRatingCount`** | 永続保存が規約違反（30日キャッシュ可なのは緯度経度のみ、恒久保存可は `place_id` のみ） |
| **OSM タグ充実度を人気の代理に使う** | 実測で反証（**伏見稲荷大社に `opening_hours` も `website` が無く、籠神社が `opening_hours` を持つ**）。測っているのはマッパー分布 |
| **ホトカミの参拝記録数の機械収集** | 同種の競合サービス。規約・著作権上のリスク。妥当性検証の参照に留める |
| スタンプラリー的UI（コンプリート率、収集バッジ） | 「御朱印は商品ではない」という参拝者の反発と一致しない |
| 祈祷・お守りの販売、有料祈祷の予約 | 収益化すると景表法・薬機法の様相が変わる。送客に徹する |
| 実行時の Overpass / Wikimedia API 呼び出し | 調査中に実際に遮断された。全て事前ETLでローカルDB化する |
| 国土数値情報 P12/P27/P32 | 非商用ライセンス。P27 にはそもそも社寺が含まれない |
| Google / Mapbox の経路API | 結果のDB保存が規約違反。Google は他社地図との併用も禁止 |
| 式内社データベース（shikinaisya.net） | 質は高いが個人利用のみ・商用不可 |
| 湯温のようなテキスト推定 | （HitoriYado の教訓）テキスト量が足りない推定は成立しない |

---

## 運用ルール（HitoriYado に準拠）

### ブランチ

- `main` … 常に `npm test` / `npm run typecheck` が緑
- `feat/<内容>` `fix/<内容>` … 作業ブランチ。**main への直接コミット・push は禁止**

### コミットメッセージ

Conventional Commits（`feat:` `fix:` `refactor:` `docs:` `test:` `chore:`）

### ドキュメントの昇格

Cowork セッションの出力は `_OUTPUTS/` に生成する（Git 管理外）。残す価値があると判断したものだけ `docs/` へ移動してコミットする。

### 秘密情報

ODPT の consumerKey は環境変数でのみ扱う。`.env.local` / `.dev.vars` は `.gitignore` 済みにする。

### 帰属表示

`docs/2026-08-31_MairiMichi_外部データ源調査報告_v1.md` §9 の文言をフッタに固定する。**消さないこと。**
